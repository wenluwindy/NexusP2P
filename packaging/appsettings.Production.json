{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "Signaling": {
    "_1_PublicOrigin": "★ 必填。对外公开的基址，用来生成分享链接。填错的话对方打不开链接，而这种错很难从现象倒推。缺了它服务会直接拒绝启动（刻意如此）。",
    "PublicOrigin": "https://p2p.你的域名",

    "_2_BehindReverseProxy": "★ 跑在 nginx / Caddy 后面就必须改成 true。不改的话入房限速会失效：所有人共用代理那一个 IP 的配额。反之，没在代理后面千万不要打开 —— X-Forwarded-For 客户端可以随便写，伪造一下就能绕过限速。",
    "BehindReverseProxy": false,

    "_3_KnownProxies": "代理在另一台机器上时填它的 IP。留空表示只信任本机（代理与本服务同机，最常见）。",
    "KnownProxies": [],

    "_4_RoomGracePeriodSeconds": "两端都断开后房间保留多久。自动重连依赖它，别调到 0。",
    "RoomGracePeriodSeconds": 60,

    "_5_JoinAttemptsPerMinute": "同 IP 每分钟入房尝试上限，防止九位码被枚举。",
    "JoinAttemptsPerMinute": 20,

    "_6_MaxRooms": "房间总数上限，防止有人靠不停建房吃光内存。",
    "MaxRooms": 1000,

    "Turn": {
      "_comment": "打洞失败时的中继兜底。配了 Urls 就必须配 Secret，且 Secret 要与 coturn 的 static-auth-secret 完全一致，否则「中继配了却永远连不上」且没有有用的报错。",
      "Urls": [],
      "Secret": "",
      "CredentialTtlSeconds": 3600
    }
  }
}
